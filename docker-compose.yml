
services:
  app:
    build:
      context: .
    container_name: test_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    networks:
      - app-network
    command: uvicorn --factory main:create_app --host 0.0.0.0 --port 8000 --reload

networks:
  app-network:
    driver: bridge